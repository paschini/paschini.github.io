kind: pipeline
type: kubernetes
name: default

steps:
  - name: dependencies
    image: node
    commands:
      - npm install
      - npm run build
  - name: publish
    image: plugins/gh-pages
    pages_directory: build/

